//wap to perform sum of two matrices
#include <stdio.h>
int main()
{
    int row, col;
    printf("enter row and column size=");
    scanf("%d%d", &row, &col);
    int arr[row][col];
    printf("enter array elements=");
    for (int i = 0; i < row; i++)
    {
        for (int j = 0; j < col; j++)
        {
            scanf("%d", arr[i][j]);
        }
    }
    //logic to perform addition of two matrix
    printf("Sum of matrix is \n");for(int i=0; i<row; i++){
        for (int i=0; i<row; i++){
           for (int j=0;j<col; j++){
            int sum =matri
           }
        }
    }

}